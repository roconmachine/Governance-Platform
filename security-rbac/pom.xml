<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.platform.security</groupId>
    <artifactId>security-rbac</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>
    <name>Security :: RBAC</name>
    <description>
        Turns "only PAYMENT_ADMIN can approve transfers over $10k" from a
        wiki page into @RequiresRole/@RequiresPermission annotations enforced
        by an AOP aspect - and turns the role-to-permission mapping itself
        into a versioned configuration property instead of scattered
        hasRole() checks nobody can audit in one place.

        Depends on spring-security-core directly and non-optionally, same as
        security-auth: reading the authenticated context and enforcing
        access decisions IS this module's purpose. Deliberately does NOT
        depend on security-auth - it only requires SOME Authentication to be
        present in Spring Security's context, from whatever populated it
        (security-auth, a different auth mechanism, or Spring Security's own
        standard filters).
    </description>

    <properties>
        <java.version>21</java.version>
    </properties>

    <dependencies>
        <!-- Small, optional-in-spirit-but-declared dependency: only used to read the
             correlation id (for denial-log context), the same lightweight integration
             pattern used by every other module in this suite. -->
        <dependency>
            <groupId>com.roconmachine</groupId>
            <artifactId>governance-core</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>

        <!-- Non-optional: enforcing access decisions against the security context IS this module's purpose -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-core</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>
