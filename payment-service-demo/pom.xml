<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.roconmachine</groupId>
    <artifactId>demo-test-app</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    <name>demo-test-app</name>
    <description>
        Sample consuming microservice exercising every module in
        governance-platform. Depends on the built SNAPSHOT artifacts exactly
        the way a real external service would (by Maven coordinates, NOT as
        a reactor sibling module) - run `mvn clean install` in
        governance-platform first so these are present in your local
        ~/.m2 repository before building this project.

        Uses governance-app-response, NOT governance-exception-handling -
        the two are documented alternatives (both are global
        @RestControllerAdvice beans; using both in one service risks
        Spring's "Ambiguous @ExceptionHandler method mapped" failure). See
        governance-app-response's README for the full comparison if you'd
        rather see governance-exception-handling wired in instead.
    </description>

    <properties>
        <java.version>21</java.version>
    </properties>

    <repositories>
        <repository>
            <id>github</id>
            <url>https://maven.pkg.github.com/roconmachine</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- ===================== governance-platform ===================== -->

        <!-- governance-core comes in transitively via every module below -->

        <dependency>
            <groupId>com.roconmachine</groupId>
            <artifactId>governance-audit</artifactId>
            <version>1.0.0</version>
        </dependency>

        <dependency>
            <groupId>com.roconmachine</groupId>
            <artifactId>governance-http-logging</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <!-- Chosen over governance-exception-handling - see class-level Javadoc above -->
        <dependency>
            <groupId>com.roconmachine</groupId>
            <artifactId>governance-app-response</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>



        <!-- ===================== security-* ===================== -->

        <dependency>
            <groupId>com.platform.security</groupId>
            <artifactId>security-crypto</artifactId>
            <version>1.0.0</version>
        </dependency>

        <dependency>
            <groupId>com.platform.security</groupId>
            <artifactId>security-auth</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>com.platform.security</groupId>
            <artifactId>security-rbac</artifactId>
            <version>0.1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>com.platform.security</groupId>
            <artifactId>security-auth-ms-exchange</artifactId>
            <version>1.0.0</version>
        </dependency>

        <!-- ===================== ordinary Spring Boot pieces this demo needs ===================== -->

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>8</source>
                    <target>8</target>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
